export interface Test {
    name: string;
    description: string;
    allocatedTime: number;
    deadline: Date;
    totalQuestion: number;
    maxScore: number;
    totalAttempt: number;
    maxAttempt: number;
}